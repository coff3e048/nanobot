version: '3.0'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: nanobot
    restart: unless-stopped
    volumes:
      -  /nanobot:/path/to/nanobot/data
    env_file:
      - .env
#   environment:
